function nineTask() {
    for (let num = 1; num <= 9; num++){
        for(let factor = 1; factor <=10; factor++){
            console.log(`${num} * ${factor} = ${num * factor}`);
        }
        console.log(`==============`); 
    }
}